## 一种SLO感知型的推理引擎调度策略

### 文档简介

为推进一个 ai infra 相关课题遂创建此文档，用于记录本课题核心理论和研究成果，亦包含个人日志与心得体会。

### 概述

此课题主要研究对象是一种新的SLO感知型的大模型推理引擎调度算法，核心目的是优化用户体验。

### 背景

大模型的推理依靠推理引擎，面对多用户高并发的推理请求任务时推理引擎调度器的表现直接决定了用户的真实体验，目前市面上大多数开源推理引擎如vllm、sglang都采取最简单的FCFS策略（也包括静态优先级调度策略），这种简单的调度策略虽然易懂但是也有很多问题，比如队头阻塞等等，后来很多工作针对其做了改进。

但随着Agent相关技术的发展，大模型不再只是一个简单的“文本补全器”或者“对话助手”，目前主流大模型都属于**增强型大语言模型**，它们通过强化学习等技术经过特殊训练和微调，具备 Tool Call 能力，这种能力表现为模型能够理解 prompt 提供的 json 格式的工具描述，并且根据用户需求正确选择要使用的工具，严格遵循描述生成符合 schema 要求的参数，之后在接收到工具执行返回的执行结果之后能理解并且最终给用户反馈。然而这种技术也意味着大模型推理的每一个token不再全都是**用户可见**的，当大模型正常进行纯文本对话时，每一个token都被detokenize 成人类可读字符并实时渲染显示在客户端界面，此时字符的生成速度和时间间隔至关重要，所以也就产生了诸如ttft、tpot等指标用于评估引擎在这方面的性能。然而，正如前文提到的，当大模型发挥 Tool Call 能力，处于生成参数的阶段时，token是“用户不可见”的，此时用户对于token的生成速度以及间隔不敏感。正是这两个阶段的截然不同的特性，使得其对于计算资源的需求也不同，然而现有的工作默认所有的 token 同等重要。

针对这一优化的角度，我提出调度的核心思路：***当用户请求处于不可见生成阶段时应进行适当“降权”处理，将有限的计算资源优先供给到那些敏感的处于用户可见阶段的请求中，在保证SLO的基础上，维持系统吞吐量不下降太多，而工具调用的端到端延迟又不劣化太剧烈***。

### 实验环境

搭载4090显卡的服务器做初步实验，后续迁移到4卡H100。基于 vllm v0.19.1 版本开发

### 数学模型

心理学概念——***韦伯定律***：**人对刺激变化的感知，受相对比例影响更大，而非绝对增量**。举个例子，假设一个航班预计2h，延误1h，人们很可能会产生抱怨；而如果一个航班预计24h，延误1h，人们往往会更容易接受。而在行为经济学中也有基于韦伯定律的衍生概念，即比例偏见，指人们在评估食物（时间、价格、收益、损失）时，往往天生更倾向于关注相对的百分比而不是绝对的数值。

应用到本课题场景中，即如果端到端延迟中用户不能看到任务的每一步变化，那么**在合理容忍度之内此任务可以拖延**。换个说法，如果用户本来就预期这个任务需要等待，且中间状态不可交互，那么系统可以在不明显破坏体验的前提下适度调整其调度频率。

定义用户一次完整推理任务（从发送请求到收到文字响应）的端到端延迟的数学模型为：

$$T_{E2E}=\sum_{i=0}^{N-1}(T_{prefill\_i}+T_{argument\_i}+T_{exec\_i})+T_{prefill\_N}$$

其中 N 为本次任务的工具调用对话的总轮数，这个数学模型无法作为优化目标，因为在系统任一时刻无法知道N的值，也无法知道每一轮模型选择什么tool，并且强行预测误差过大，所以做合理的工程近似，优化目标退化到单轮：
$$ T_{turn\_i}=T_{prefill\_i}+T_{argument\_i}+T_{exec\_i}$$

只研究 single turn，也就是一次以 request 为粒度的过程，为简便可以去掉下标 i

$$T_{turn}=T_{prefill}+T_{decode/argument}+T_{exec}$$



**那么如果说 $T_{turn}$ 越大，那么根据韦伯定律，在这一轮中容忍拖延的时间就越多**，这拖延的时间不是浪费掉，而是给其他更加紧急的请求使用。另一种说法是**在可容忍的程度内，从一部分请求中“偷取”算力给另一部分用**。

根据定律， $T_{turn}$ 越大，容忍的绝对延迟就越多，定义公式：

$$T_{delay}=f(T_{turn})$$

其中 f 是单调递增函数，对这个函数的建模可以有多种形式，这里给出最简单的线性模型：

$T_{delay}=\alpha T_{turn}$

或者保守一点 $T_{delay}=min(\alpha T_{turn}, C)$

### 优先级因子

优先级需要三个因子参与构成：1. $T_{prefill}$ 2. $T_{decode/argument}$ 3. $T_{exec}$ 

显然如果等到这三个因子产生的时候，本轮已经结束了，调度也就没有了意义。所以需要选取 turn 生命周期中的某个时机或某几个时机对因子进行预测，时机可能是 req 刚进入系统或者是某个阶段转换的时刻。

公式改写成：

$$\begin{align*}T_{delay}&=f(T^{est}_{turn})\\ &=f(T^{est}_{prefill}+T^{est}_{argument}+T^{est}_{exec})\end{align*}$$

$T_{prefill}$  通常是一次性计算，和 prompt 长度、KV cache 状态、batching、GPU负载有关。它可预测性较强，因为输入 token 数已知。甚至可以不预测，如果预测时机在 prefill 阶段之后，那么 $T^{est}_{prefill}$ 就等于记录下来的真实 $T_{prefill}$

$T_{argument}$ 位于 decode 阶段，调度器可以通过 decode step 的分配频率影响它的完成时间。但 argument 长度未知，所以它是主要不确定性来源。

$T_{exec}$  是外部工具执行时间，LLM serving 调度器通常不能直接缩短它。但它仍然应该进入优先级计算，因为它影响用户总体等待感知。一个 tool 本身很慢的请求，可能拥有更大的相对拖延空间

### 预测算法

现在不考虑 $T^{est}_{prefill}$，对于 $T^{est}_{argument}$ 和 $T^{est}_{exec}$ 的获取，通常可以从几个层次处理：

**使用先验/历史信息**：不同 tool 的 argument 长度以及执行时间分布差异很大，比如 search 类工具、calculator 类工具、code execution 类工具、browser 类工具等等

**使用 context 特征**：context 对预测值具有决定作用

**在线修正**：decode 开始后，已经生成的 argument token 数会逐步暴露，可以根据当前状态估计剩余长度

根据以上思路，我提出一个算法，也即**基于模型自举的预测算法**：大模型在 tool call 时一般会 decode 出两部分信息，name 和 argument ，此算法本质上是通过修改 chat template 强制要求 decode 过程在 decode argument 之前先行 decode 出调度辅助信息，可以是 expected_argument_len 和 expected_tool_exec_time。

此外还可以结合历史信息，比如在 chat template 里面加入相关 tool 的历史信息，或者用历史值修正模型预测值：$$T^{est}=\lambda T_{model}+(1-\lambda)T_{history}$$

这种算法无需外接任何预测模块，也无需额外训练或者微调小模型，仅凭借大模型自身的语义分析与理解能力实现精确预测

### 调度算法

一个朴素的想法是：可拖延时间 $T_{delay}$ 越高，该请求的优先级越低。调度系统中当能够确定 $T_{prefill},T_{argument},T_{exec}$ 三者时，该 req 优先级唯一确定，使用该优先级指导系统中全部 req 的调度顺序，这是一种基于静态优先级的调度方案。

然而这种静态方案是有问题的，优先级一旦确定下来讲不再改变，优先级高的 req 往往会更早被调度完成，最大的问题就是**无法充分耗尽 $T_{delay}$ **，优先级低的 req 可能一直被压制，优先级高的 req 太早完成，导致 req 的可拖延预算没有被平滑地使用，可能优化效果不佳。

所以需要使用动态优先级，可以给每个请求定义一个“软 deadline”：

$$D=t_{arrival}+T^{est}_{turn}+T_{delay}$$

在任意时刻 $t$，定义剩余松弛时间：

$$Slack(t)=D-t-T^{est}_{remaining}(t)$$

其中 $T^{est}_{remaining}$ 指 req 从此刻举例完成预计还需要的时间， $T^{est}_{remaining}=T^{est}_{turn}-T_{served}$ 

带入化简可以得到：

$$\begin{align*}Slack(t)&=T_{delay}-[t-(t_{arrival}+T_{served})]\\&=T_{delay}-T_{not\_served}\end{align*}$$

化简之后的公式便于工程开发实现

直觉是：

如果 Slack(t) 很大，说明这个请求还有很多可拖延空间，可以降低优先级。

如果 Slack(t) 接近 0，说明再拖就会超过用户容忍范围，应该提高优先级。

如果 Slack(t)<0，说明已经违约，需要最高优先级或进入补救策略。

所以也可称为**最低松弛度优先（LLF）**

### Benchmark

采用 SweBench + ShareGPT 混合数据集，具体来说，我使用了我自己开发的另外两个项目作为辅助：

1. Chat-Replayer

此项目专门对 ShareGPT 格式的 Session 数据进行“回放”，在此过程中记录并计算 ttft tpot latency 等一切指标，每一个独立 session 启动一个 replayer 实例运行，多个实例由多线程运行，实例的派发间隔分布服从泊松分布

2. Pico-Agent

此项目负责跑 Swebench 的 dataset ，搭配 docker 环境，同样记录指标，只不过由于模拟的是工具调用请求，故不记录 token 粒度的指标比如 ttft 和 tpot，负载管理层面采取同样的多线程和泊松分布

### 优化目标

优化的核心指标分为三组：

- 对 ShareGPT 可见输出请求，看 TTFT、TPOT、P95 latency、P99 latency
- 对 tool call 请求，看 E2E latency
- 对系统整体，看吞吐量、SLO violation rate。

Baseline 暂且设置为 vllm 默认的 FCFS 调度

预期实验结果应该是：相较于 Baseline 而言，ShareGPT格式的纯对话请求的各项指标都获得一定程度的优化，而 SweBench dataset 的 tool call 请求的延迟应该会有合理程度下的增加，而系统整体吞吐量不应有太大变动，SLO violation rate 明显降低